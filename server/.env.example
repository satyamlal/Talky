## Server configuration
# Port the WebSocket/HTTP server listens on
PORT=8080

# Set to 'production' when deploying
NODE_ENV=development

## Resend configuration for OTP emails
# Your Resend API key (required for sending OTP)
RESEND_API_KEY=

# Optional Resend API base and From header
RESEND_API=
RESEND_FROM="Talky <onboarding@resend.dev>"

# Note: For development only, the server currently accepts the universal OTP '0000'.
# This is a temporary override to unblock flows when email delivery is flaky.
# Remove this override in code before deploying to production.
